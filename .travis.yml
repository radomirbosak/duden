language: python
python:
  - "3.4"
  - "3.6"
  - "nightly"
install:
  - "pip install -r requirements.txt"
# command to run tests
script:
 - python --version
 - green tests/ --quiet-stdout
 - autopep8 -r . --diff
 - find -name '*.py' | xargs flake8
